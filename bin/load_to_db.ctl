OPTIONS (SKIP = 1, DIRECT = TRUE, BINDSIZE = 1024000, ERRORS = 99999999)

LOAD DATA
  TRUNCATE
    INTO TABLE geoip_cityisporg_for_load
      FIELDS TERMINATED BY '|' TRAILING NULLCOLS
(
  STARTIP,
  STARTIPNUM,
  ENDIP,
  ENDIPNUM,
  LOCID,
  COUNTRY,
  REGION,
  CITY,
  POSTALCODE,
  LATITUDE,
  LONGITUDE,
  METROCODE,
  AREACODE,
  ISP,
  ORGANIZATION,
  CREATED_DATE SYSDATE
)
